
/**
 * {{summary}}
 */
export interface {{identToPascalCase system}}{{identToPascalCase name}} {
  {{#each properties}}
  /**
   * {{summary}}
   */
  {{name}}{{#if isOptional}}?{{/if}}: {{>propertyType .}}{{#if isArray}}[]{{/if}}

  {{/each}}
}
