/**
 * Represents a system of Jsonotron enum and schema types.
 */
export interface JsonotronSystem {
  /**
   * The domain of a system.
   */
  domain: string

  /**
   * The name of a system.
   */
  system: string
}
