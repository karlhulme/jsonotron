{{~#if isBool ~}}
  boolean
{{~/if}}
{{~#if isEnum ~}}
  string
{{~/if}}
{{~#if isFloat ~}}
  number
{{~/if}}
{{~#if isInt ~}}
  number
{{~/if}}
{{~#if isObject ~}}
  Record<string, unknown>
{{~/if}}
{{~#if isRecord ~}}
  {{identToPascalCase propertyTypeSystem}}{{identToPascalCase propertyTypeName}}
{{~/if}}
{{~#if isString ~}}
  string
{{~/if}}