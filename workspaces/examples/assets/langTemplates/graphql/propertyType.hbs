{{~#if isBool ~}}
  Boolean
{{~/if}}
{{~#if isEnum ~}}
  {{identToPascalCase propertyTypeSystem}}{{identToPascalCase propertyTypeName}}
{{~/if}}
{{~#if isFloat ~}}
  Float
{{~/if}}
{{~#if isInt ~}}
  Int
{{~/if}}
{{~#if isObject ~}}
  JSON
{{~/if}}
{{~#if isRecord ~}}
  {{identToPascalCase propertyTypeSystem}}{{identToPascalCase propertyTypeName}}
{{~/if}}
{{~#if isString ~}}
  String
{{~/if}}